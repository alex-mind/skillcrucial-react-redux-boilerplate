const initialState = [
  {
    name: '',
    image: '',
    hashtag: ''
  }
]

// Список пользователей

// Имя
// Картинка
// ХешТег (#ovasylenko)

export default function(state = initialState, action) {
  switch (action.type) {
    default: {
      return state
    }
  }
}